export default function DataExportPage() {
	return <div className="space-y-6"><div><h1 className="text-3xl font-bold">Data export</h1><p className="mt-1 text-gray-500">Download the records belonging to this store workspace.</p></div><section className="glass-card max-w-2xl p-6"><h2 className="text-xl font-semibold">Tenant export</h2><p className="mt-3 text-sm text-gray-600">The export includes store settings, catalog, orders, payment status, reviews, coupons, memberships, domains, and support records that are currently tenant-scoped.</p><p className="mt-3 text-sm text-amber-700">Secrets, passwords, invitation tokens, provider references, verification documents, and payment metadata are intentionally excluded. After paid access ends, merchant data follows the configured 90-day retention schedule; SaaS billing and legal records are retained separately.</p><a className="btn-primary mt-6 inline-flex" href="/api/manage/data-export">Download JSON export</a></section></div>
}
